[Unit]
# Start the public proxy and the application process manager independently.
# HAProxy can accept connections while PM2 restores the application, then use
# its bounded connection retries during application startup.
Wants=pm2-root.service
After=network-online.target
